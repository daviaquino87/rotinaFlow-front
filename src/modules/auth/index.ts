// Auth module — authentication is handled server-side via session cookies.
// Client-side auth state is read from the /api/session endpoint via @/api-client.
// Re-export from here as the auth module grows.
export {};
